# Copy to `.env` for `vercel dev` (Vercel CLI reads `.env`, not only `.env.local`).
# Never commit real secrets. Both `.env` and `.env.local` are gitignored.
COMPILER_BASE_URL=https://example-compiler.invalid
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_BUCKET=new-assets.ccbp.in
S3_PREFIX=testing-coding-question-test-cases/
